# Public recursive resolvers used only inside the untrusted build sandbox.
# Keeping DNS off the host loopback lets the sandbox deny every local/private
# address, including Redis, Postgres, cloud metadata and Caddy control planes.
nameserver 1.1.1.1
nameserver 8.8.8.8
options edns0 timeout:2 attempts:3
