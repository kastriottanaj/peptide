# Plan — IndexNow submission

Spec: [../specs/2026-07-30-indexnow.md](../specs/2026-07-30-indexnow.md)

## 1. Key file route

- [x] `storefront/src/pages/[indexnowKey].txt.ts` — `getStaticPaths` yields one
      path when `INDEXNOW_KEY` is set and valid (`^[A-Za-z0-9-]{8,128}$`), none
      otherwise. Body is the key, `Content-Type: text/plain`.
      Consumes: `INDEXNOW_KEY`. Produces: `/<key>.txt` in `dist/`.
- [x] `storefront/.env` — a locally generated key, so the route is exercised.

## 2. Submitter

- [x] `storefront/scripts/indexnow-submit.mjs` — reads `dist/sitemap-*.xml`,
      resolves each `<loc>` to its built HTML file, hashes it, diffs against the
      state file, POSTs the changed URLs to `api.indexnow.org`.
      Preflight skips (exit 0): no key, non-https origin, key file not publicly
      readable, nothing changed. Flags: `--dry-run`, `--all`, `--state`,
      `--dist`.
      Consumes: `INDEXNOW_KEY`, `PUBLIC_SITE_URL`, `dist/`.
      Produces: `.indexnow-state.json`.
- [x] `storefront/package.json` — `"indexnow": "node scripts/indexnow-submit.mjs"`.
- [x] `storefront/.gitignore` — ignore `.indexnow-state.json`.

## 3. Deploy wiring

- [x] `deploy/deploy.sh` — write `INDEXNOW_KEY` into the build `.env`; add a
      non-fatal submit step after the `rsync` publish, with the state file at
      `/srv/peptides/indexnow-state.json`.
- [x] `deploy/caddy.env.template` — untouched (Caddy needs nothing).

## 4. Docs

- [x] `docs/indexnow.md` — runbook.
- [x] `AGENTS.md` — pointer from the existing IndexNow bullet.
- [x] `README.md` — `INDEXNOW_KEY` in the storefront `.env` block.
- [x] `docs/go-live-checklist.md` — launch step under §7.
- [x] `CREDENTIALS.local.md` — record the key (not committed).

## 5. Verify + commit

- [x] `cd storefront && npm run typecheck && npm run build`
- [x] `ls dist/*.txt` shows `<key>.txt`; `curl` the dev server for it
- [x] `npm run indexnow -- --dry-run` skips on a localhost origin
- [x] Same command against a production-origin build skips with "key file not
      reachable" (the gate), proving the pipeline declines to submit
- [x] No raw hex added to CSS (no styling in this change), `git status --short`
      clean of foreign changes, commit
